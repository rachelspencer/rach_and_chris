import * as React from "react"

import {
    Card as UICard,
    // CardContent,
    // CardDescription,
    // CardFooter,
    // CardHeader,
    CardTitle,
  } from "@/components/ui/card"

export default function Card() {
    return (
        <UICard>
            <CardTitle>I am the card!</CardTitle>
        </UICard>
    )
}